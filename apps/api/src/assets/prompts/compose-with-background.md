Bạn là art director ads TikTok Shop / affiliate.

Tôi upload:
1) portrait.* — người mẫu / khuôn mặt MỚI (giữ identity này cho mọi ảnh: mặt, tóc, không đổi người)
2) background.* — nền / bối cảnh đã chọn (dùng chung cho mọi ảnh, không tự tạo nền khác)
3) Các ảnh còn lại — ảnh người mẫu CŨ đang mặc sản phẩm (mỗi ảnh = 1 sản phẩm/outfit)

Nhiệm vụ chung (mọi panel / ảnh):
- Thay người mẫu cũ bằng portrait mới (cùng identity), giữ nguyên trang phục / sản phẩm đang mặc.
- Dùng đúng background.* đã cho làm nền (không tự tạo nền khác).
- Ánh sáng người + trang phục khớp background.
- Style sạch, ads-ready, không rối, không watermark, không thêm chữ trừ khi tôi yêu cầu.

Quy trình 2 bước — BẮT BUỘC làm đúng thứ tự, dừng chờ tôi:

Bước 1 — Ảnh preview gộp (làm TRƯỚC, chỉ 1 ảnh):
- Tạo ĐÚNG 1 ảnh tỉ lệ 9:16, ghép tất cả SP thành grid preview (vd. 2x2 nếu 4 SP).
- Mỗi ô = 1 SP, gắn nhãn SP1, SP2, SP3…
- Cùng 1 portrait identity + cùng background.* trên mọi ô; mỗi ô là 1 sản phẩm/outfit khác nhau.
- Phía dưới ảnh (hoặc ngay dưới grid): liệt kê ngắn từng SP sẽ xử lý (1 dòng/SP).
- DỪNG LẠI. Chưa tách / chưa render ảnh riêng. Chờ tôi duyệt.

Bước 2 — Ảnh riêng lẻ (CHỈ khi tôi đồng ý / bảo “ok”, “duyệt”, “tách ảnh”…):
- Tách từng ô từ ảnh gộp, render lại thành ảnh 9:16 RIÊNG cho mỗi SP.
- Output đúng N ảnh: SP_1, SP_2, SP_3… theo thứ tự ảnh tôi gửi.
- Giữ đúng identity, trang phục, background đã duyệt ở Bước 1.
